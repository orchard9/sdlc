Researched url-entry: a UAT fixture for the --from-url flag introduced in v10-knowledge-capture. Traced implementation through crates/sdlc-cli/src/cmd/knowledge.rs (add() fn, fetch_page_title() fn using ureq) and crates/sdlc-core/src/knowledge.rs (SourceType, OriginKind, Source structs). Documented the full web-origin pattern: HTTP fetch, title extraction, Source provenance record, origin=web. Confirmed entry is an orphan UAT fixture — no real analysis, just example.com placeholder. Enriched content.md with full pattern documentation and usage examples.