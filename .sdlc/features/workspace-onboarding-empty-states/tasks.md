# Tasks: Rich Onboarding Empty States

## T1: Ponder page empty state
Replace the minimal "no selection" placeholder in `PonderPage.tsx` with the structured Hero / How-it-works / Lifecycle / CTA layout. The Ponder page already had this change applied as part of a prior iteration.
- **Status**: done

## T2: Root Cause (Investigation) page empty state
Replace the minimal "Select an investigation" placeholder in `InvestigationPage.tsx` with Hero (Microscope icon, "Find the root cause"), 4-step How-it-works (Target, Layers, FileText, CheckCircle2), and "New Root Cause" CTA button.
- **Status**: done

## T3: Guidelines page empty state
Replace the minimal "Select a guideline" placeholder in `GuidelinePage.tsx` with Hero (ScrollText icon, "Codify what works"), 4-step How-it-works (Search, Scale, FileEdit, BookOpen), and "New Guideline" CTA button.
- **Status**: done

## T4: Spikes page empty state
Replace the minimal detail pane in `SpikePage.tsx` with Hero (FlaskConical icon, "Answer one question fast"), 3-step How-it-works (HelpCircle, Beaker, Scale), Verdicts strip (ADOPT/ADAPT/REJECT), and CLI command CTA.
- **Status**: done

## T5: Knowledge page empty state
Replace the minimal "Select an entry to read" placeholder in `KnowledgePage.tsx` with Hero (Library icon, "What the team knows"), 3-step How-it-works (BookOpen, Search, Layers), and CLI command CTA.
- **Status**: done
