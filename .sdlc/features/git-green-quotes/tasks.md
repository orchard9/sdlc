# Tasks: git-green-quotes

## T1: Create quote corpus module
Create `frontend/src/lib/quotes.ts` with the `Quote` interface, `QUOTES` array (12+ entries), and `getWeeklyQuote()` selector function.

## T2: Create GitGreenQuote component
Create `frontend/src/components/GitGreenQuote.tsx` — a presentational component that renders a quote as a styled blockquote with author attribution.

## T3: Unit tests for quote logic and component
Add tests for `getWeeklyQuote` (determinism, rotation) and `GitGreenQuote` (renders text and author).
