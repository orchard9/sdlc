# Tasks: hub-create-repo-ui

1. Add `CreateRepoResponse` type to `frontend/src/lib/types.ts`
2. Add `createRepo()` to `frontend/src/api/client.ts`
3. Add `CreateRepoSection` component to `HubPage.tsx` (two-step: name form → instructions)
4. Wire `CreateRepoSection` into the fleet view in `HubPage.tsx` as "Add New Project" section
