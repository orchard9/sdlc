# Tasks: Fix Investigation Session Completion

1. Add --output-type flag to CLI investigate update command
2. Allow --output-ref for evolve investigations in CLI (append to output_refs Vec)
3. Allow output_type and output_ref for evolve investigations in REST API
4. Add tests for the new CLI and REST behavior
