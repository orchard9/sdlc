# Tasks: UatHistoryPanel

## Task List

1. **Create `frontend/src/components/milestones/` directory** — ensure the directory exists for the new component
2. **Create `UatHistoryPanel.tsx` component** — implement the full component with props, fetch on mount, sorted run list, verdict badges, empty state, and loading state; add `data-testid="uat-history-panel"` to the root element
3. **Update `MilestoneDetail.tsx`** — import `UatHistoryPanel` and render it in a new "UAT History" section below the Features section
